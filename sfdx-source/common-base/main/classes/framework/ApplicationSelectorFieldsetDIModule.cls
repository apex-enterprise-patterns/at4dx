public class ApplicationSelectorFieldsetDIModule
    extends di_Module
{
    public override void configure() 
    {
        // for loop from TestDataExtension_mdt records {

            // bind(SObjecType);
            // bind(ITestDataExtension.class);
            // data(mdt_record);
            // to(mdt_record.To__c);

        //}
    }
}
