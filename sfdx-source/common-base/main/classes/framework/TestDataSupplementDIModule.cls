public class TestDataSupplementDIModule
{
    public void configure()
    {
        // for loop from TestDataSupplementer_mdt records {

            // bind(SObjecType);
            // bind(ITestDataSupplementer.class);
            // data(mdt_record);
            // to(mdt_record.To__c);

        //}
    }
}
